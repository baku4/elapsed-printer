[package]
name = "elapsed-printer-test"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
elapsed-printer = { path = "../elapsed-printer" }

[features]
feature_1 = []
feature_2 = []
feature_3 = []
